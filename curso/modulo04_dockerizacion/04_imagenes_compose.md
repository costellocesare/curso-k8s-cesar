# Creando imágenes y usando Docker Compose

Aprende a construir imágenes y a usar Docker Compose para levantar varios servicios localmente.

```bash
docker build -t mi-backend .
docker-compose up
```
