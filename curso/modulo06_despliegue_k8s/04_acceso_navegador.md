# Accediendo a la app desde el navegador

Cómo acceder a tu aplicación desde el navegador usando el NodePort o el servicio expuesto.

- Verifica la IP de Minikube: `minikube ip`
- Accede a `http://<minikube-ip>:30001`
